{
	"name": "cantine-backoffice-manager",
	"version": "1.0.0",
	"type": "module",
	"scripts": {
		"test": "node --test",
		"test:watch": "node --test --watch",
		"format": "npx @biomejs/biome format --write .",
		"lint": "npx @biomejs/biome lint --write .",
		"prepare": "copy .husky\\pre-commit .git\\hooks\\pre-commit",
		"deploy": "clasp push"
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.8"
	}
}
